"""Card matching, confidence and evidence assembly (roadmap M3).

The card *schema* lives here rather than in ``knowledge/`` for the reason
:mod:`genetics.refs.manifest` gives for the reference manifest: the data is authored by
hand and the rules about it have to be executable, so that a mistake fails at load rather
than at render.
"""
